import React from "react";

interface RegistrationCompProps {}

const RegistrationComp = (props: RegistrationCompProps) => {
  return (
    <div>
      <p>RegistrationComp</p>
    </div>
  );
};

export default RegistrationComp;
